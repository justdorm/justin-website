{
  "name": "justin-website",
  "version": "1.0.0",
  "description": "Justin Dormitzer's personal website",
  "scripts": {
    "start": "node createCategoryPages.js && jus serve ./src",
    "build": "node createCategoryPages.js && jus compile ./src ./docs",
    "deploy": "npm run build && && git add . && git commit -m \"Automated build commit\" && git push origin master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/justdorm/justin-website.git"
  },
  "author": "Jeremy Dormitzer <jeremy.dormitzer@gmail.com>",
  "devDependencies": {
    "jus": "^0.21.0"
  }
}
